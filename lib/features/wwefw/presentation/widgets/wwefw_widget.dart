import 'package:flutter/material.dart';

class WwefwWidget extends StatelessWidget {
  const WwefwWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
